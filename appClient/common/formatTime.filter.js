(function () {

  angular
    .module('blogsiteApp')
    .filter('formatTime', formatTime);

  function formatTime () {
    return function(time){
      var numTime,unit;
      //if (time && )
    };
  }

})();